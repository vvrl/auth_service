server:
  port: "8080"

logger:
  level: "debug"
  filename: "./server.log"

database:
  driver: "postgres"
  host: "auth-psql"
  port: 5432
  user: "auth_user"
  password: "auth_password"
  dbname: "auth_db"
  maxAttempts: 20